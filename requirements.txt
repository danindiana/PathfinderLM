# Core AI/Retrieval Dependencies (local-first)
# Generation + embeddings run on the Ollama 0.22.1 runtime via its HTTP API,
# so no torch/transformers are required for the default path.
ollama>=0.3.0          # Python client for the Ollama runtime
faiss-cpu>=1.7.4       # Vector similarity search

# --- Optional: cloud / HuggingFace fallback ---
# Uncomment these only if you set LLM_PROVIDER=openai or run embeddings locally
# with sentence-transformers instead of Ollama.
# torch>=2.0.0
# transformers>=4.30.0
# sentence-transformers>=2.2.2
# openai>=1.0.0

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0

# Logging & Monitoring
prometheus-client>=0.17.0

# Development Tools (optional, installed via make install-dev)
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.7.0
# isort>=5.12.0
# flake8>=6.1.0
# pylint>=2.17.0
# mypy>=1.5.0
# bandit>=1.7.5
# safety>=2.3.5
